salário = float(input('Digite seu salário: R$ '))
porcentagem = float(input('Digite a porcentagem: '))

aumento = salário + (salário * porcentagem/100)

print(f'Seu salário novo é: R$ {aumento:.2f}')

