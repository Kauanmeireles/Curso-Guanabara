salário = float(input('Digite seu salário: '))
porcentagem = float(input('Digite a porcentagem: '))

aumento = salário * (porcentagem/100)
novo_salario = salário + aumento 

print(f'Seu salário novo é: R$ {novo_salario:.2f}')

